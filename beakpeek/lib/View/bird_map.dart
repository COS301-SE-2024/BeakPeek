import 'package:flutter/material.dart';

class BirdMap extends StatelessWidget {
  const BirdMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('here'),
    );
  }
}
